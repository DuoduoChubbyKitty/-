# 2026-08-11 工作日志

## C++ 重构巡查第 53 轮（01:11）—— C++ 仍停摆 / Swift OCR 单点继续迭代

- 自动化 `automation-1783884095474` 触发；`cpp/`、`src/cpp_bridge.py` 仍缺失，C++ 无新批次。
- Swift 主线出现新批次：`SpeedOCRReader.swift` 由 23:47 更新至 00:35（289→373 行），三部署目标同 00:35:57 重建。
- `swift build -c release` 绿；Package.swift sources 已注册 SpeedOCRReader.swift。
- 详细审查结论写入 `.workbuddy/memory/2026-07-13.md` 同轮条目；本轮全程只读，未改项目源文件。


## C++ 重构巡查第 54 轮（02:13）—— 双线均无新批次，按指令空转

- C++ 三源仍全缺（`cpp/` 不存在、`src/cpp_bridge.py` 不存在、`.cpp/.h/CMakeLists` 计 0、目标 `.so` 无）→ 停摆第 54 轮，未 cmake、未跑冒烟。
- Swift 主线**本轮无新批次**：`SpeedOCRReader.swift` 仍 00:35:18、`AuroraDriveApp.swift` 仍 23:01、`Package.swift` 仍 22:54，近 90 分钟项目内零改动；三部署目标仍同 00:35:57，未重建未重签（TCC 授权不受影响）。
- 状态复核：HEAD 仍 `260689b`；git 漂移 `3140 D / 14 M / 56 ??`（?? 由 55→56，非源码漂移）；`^ D cpp/` 稳定 59；救援资产 sha256 全对齐（.so `dbc9a246…`、smoke `70dccc8d…`，`.workbuddy/memory/cpp_rescue/` 仍是唯一存活拷贝，/tmp 副本已被系统清理）。
- 第 53 轮 6 条待修项**逐条复核仍全部在位**（文件未动）：P1 临时 PNG 转储（L331-372，每 1s 写 `/tmp/ocr_full_dump.png`+`ocr_roi_dump.png`）；P1 OCR 无 5Hz 节流（dumpInterval 只节流转储、不节流推理）；P0-lite `AuroraDriveApp.swift:628` 仍"遥测模拟"假车速、OCR 真值未接线；P2 `reset()` 竞态；P2 `parse()` 超范围无回退；P2 `cropROI` 行序翻转待前台验证。
- 全程只读，未改任何项目源文件。第 40 次建议：把巡查对象正式改为 Swift 主线，或停用本自动化。


## C++ 重构巡查第 55 轮（10:20）—— C++ 停摆 55 轮 / Swift 主线大批次，上轮 6 条待修全部闭环

- C++ 三源仍全缺（`cpp/` 与 `src/cpp_bridge.py` 不存在、.cpp/.hpp/CMakeLists 计 0、产物无）→ 未 cmake、未跑冒烟；`/tmp/smoke_cpp.py` 已被系统清理，`.workbuddy/memory/cpp_rescue/` 仍是唯一存活副本。
- Swift 主线 02:18~02:24 新批次：`SpeedOCRReader.swift`(373→308 行整体重构)、`CaptureEngine.swift`、`AuroraDriveApp.swift`、`DegradeStateMachine.swift`；新增 `docs/BUGSCAN-20260811.md`（架构师 B01~B19 扫描报告）。`swift build -c release` 绿，三部署目标 02:26/02:28 一致、adhoc 签名有效（本轮未重建未重签，TCC 不受影响）。
- **已修（上轮 6 条全部闭环）**：临时 PNG 转储已删；OCR 加 5Hz 时间闸；真车速接线（effectiveSpeed/speedValid 贯通 infer×2 + 状态机 + 脱困，模拟遥测与随机抖动删除）；reset() 加 generation 防迟到结果；parse() 加范围过滤 + 双 Pass 回退；cropROI 改 CIImage 路径。额外：原生帧 use-after-release 已用 CVPixelBufferPool 自持拷贝修掉；状态机加 speedValid 门控。
- **待修（新发现 P1）**：OCR 单帧失败会把 speedKmh 写成 -1 → ocrFresh 立刻 false → `effectiveSpeed *= 0.9` 每 tick（30Hz），200ms 掉到 53%、1s 掉到 4.2%。后果：M9 vehicle_state[0] 吃假掉速；`escapeController.update` 未被 speedValid 门控、退出阈值 8km/h 永不满足 → 叠加 B06（超时置 .done 后主循环下一帧立刻 enter()）形成无限脱困循环。
- **仅标记（重要）**：BUGSCAN 的 P0-2「E2E 输入垂直翻转」**经实证为误报** —— `/tmp/flip_probe.swift` 复刻 preprocessImage 绘制，源图顶行白/底行黑 → buffer row0=255 即顶部，**无翻转**。翻转只发生在 UIKit 已翻转上下文；照报告加 translateBy/scaleBy 会反向引入真 bug，建议关闭该条。另实测主线程 cropImage 仅 0.80ms 中位（5Hz 占用 0.4%），无需改动。
- 既有待修复核仍在位：B04 前台无校验(P0)、B06 脱困无限循环(P1)、B05 专家模式顶键(P1)、B08 capture start/stop 竞态(P1)、B03 后半 YOLO 双缓冲(P1)。
- 全程只读，探针一律落 /tmp，未改任何项目源文件。第 41 次建议：巡查对象正式改为 Swift 主线，或停用本自动化。

## 11:21 C++ 重构巡查（第 56 轮）—— 双线均无新批次，空转
- **C++ 停摆第 56 轮**：`cpp/`、`src/cpp_bridge.py`、`cpp/build/autodrive_core.cpython-311-darwin.so` 三者仍全部不存在；全库 `.cpp/.hpp/CMakeLists.txt` 计 0。未编译、未跑冒烟（`/tmp/smoke_cpp.py` 亦不存在，唯一存活拷贝仍在 `.workbuddy/memory/cpp_rescue/`）。
- **Swift 主线无增量**：`find -newermt '75 minutes ago'`（排除 .git/.build/.workbuddy/models）零命中；`SpeedOCRReader.swift` 仍 02:18:24、`CaptureEngine.swift` 02:23:38、`AuroraDriveApp.swift` 02:24:03、`DegradeStateMachine.swift` 02:24:38、`docs/BUGSCAN-20260811.md` 02:19:42 —— 与第 55 轮审查时逐字节同一批，故上轮结论按定义整体沿用，未重复 grep。
- 状态复核：HEAD 仍 `260689b`；git `3140 D / 14 M / 57 ??`（?? 56→57，构建缓存可见项，非源码漂移），`^ D cpp/` 稳定 59；三部署目标仍 02:26/02:28（本 Agent 未 build/cp/codesign，TCC 授权不受影响）；救援资产 sha256 双对齐（.so `dbc9a246…`/468440B、smoke `70dccc8d…`/4903B）。
- 第 4 步高风险核对（-ffast-math/isnan、GIL 边界、S2/S3/S4/S6/S8/M2 保真、grad_accum/ego_accel_long/黑屏兜底 3 条回归、render_cameras 原地缓冲+float32）：**因无源码全部"不适用"，非"通过"**。
- 待修清单沿用第 55 轮，零新增零闭环：P1 OCR 失败 -1 → effectiveSpeed 0.9/tick 几何衰减（污染 M9 state[0] + 叠加 B06 无限脱困）；P0 B04 前台无校验；P1 B06/B05/B08/B03后半。仅标记：BUGSCAN B02「E2E 垂直翻转」已实证为误报，建议关闭。
- 全程只读，未改任何项目源文件。**第 42 次建议**：巡查对象正式改为 Swift 主线，或停用本自动化。

## 12:19 C++ 巡查（第 57 轮）— 双线零增量，空转

- **C++ 停摆第 57 轮**：`cpp/`、`src/cpp_bridge.py`、`cpp/build/autodrive_core.cpython-311-darwin.so` 三源仍全缺；全库 `*.cpp/*.hpp/CMakeLists.txt` 计 **0**。步骤 1 判定"无更新"→ 未 cmake、未编译、未跑冒烟。
- **Swift 主线无增量**：`find -newermt '70 minutes ago'`（排除 .git/.build/.workbuddy/models）**零命中**；14 个 .swift + `docs/BUGSCAN-20260811.md` mtime 与第 55/56 轮逐一相同（SpeedOCRReader 02:18:24 / CaptureEngine 02:23:38 / AuroraDriveApp 02:24:03 / DegradeStateMachine 02:24:38 / BUGSCAN 02:19:42）→ 第 55 轮审查结论整体沿用，未重复 grep。
- 状态复核：HEAD 仍 `260689b`；git `3140 D / 14 M / 57 ??`（与上轮完全一致，无漂移），`^ D cpp/` 稳定 59；三部署目标仍 `.build/release` 02:26:01 / `AuroraDriveUI` 02:28:29 / `.app` 02:28:30（本 Agent 未 build/cp/codesign，TCC 授权不受影响）；救援资产 sha256 双对齐（.so `dbc9a246…`/468440B、smoke `70dccc8d…`/4903B，`.workbuddy/memory/cpp_rescue/` 仍唯一存活拷贝）。
- 第 4 步高风险核对（-ffast-math/isnan、GIL 边界、S2/S3/S4/S6/S8/M2 保真、grad_accum/ego_accel_long/黑屏兜底 3 条回归、render_cameras 原地缓冲+float32）：**因无源码全部"不适用"，非"通过"**。
- **问题清单**：已修 0；待修沿用 —— P1 OCR 失败 -1 → effectiveSpeed 0.9/tick 几何衰减（污染 M9 state[0]，叠加 B06 成无限脱困）、P0 B04 前台无校验、P1 B06/B05/B08/B03 后半；仅标记 —— BUGSCAN B02「E2E 垂直翻转」已实证误报，建议关闭。
- 全程只读，未改任何项目源文件。**第 43 次建议**：巡查对象改为 Swift 主线，或停用本自动化。

## 13:16 巡查（第 58 轮）— 双线均无新批次，只读空转
- **C++ 线**：三源全缺（cpp/、src/cpp_bridge.py、.so），C++ 源文件计数 0 → 停摆第 58 轮，未编译未冒烟。
- **Swift 主线**：`find -newermt '70 minutes ago'`（排除 .git/.build/.workbuddy/models）**零命中**；14 个 .swift + Package.swift 的 mtime 与第 55/56/57 轮逐一相同（SpeedOCRReader 02:18 / CaptureEngine 02:23 / AuroraDriveApp 02:24 / DegradeStateMachine 02:24，其余更早）→ 第 55 轮审查结论整体沿用，未重复 grep。
- 状态复核：HEAD 仍 `260689b`；git `3140 D / 14 M / 57 ??`，与第 57 轮完全一致（无漂移）；三部署目标 mtime 仍 `.build/release/AuroraDrive` 02:26:01、`AuroraDrive.app/.../AuroraDrive` 02:28:29、`AuroraDriveUI` 02:28:30 —— 本轮未 build/cp/codesign，**TCC（屏幕录制/辅助功能）授权不受影响**。
- 待修清单零新增零闭环：P1 OCR 失败返回 -1 → effectiveSpeed 0.9/tick 几何衰减（污染 M9 `state[0]`，并与 B06 无限脱困叠加）；P0 B04 注入前未校验前台应用；P1 B06 / B05 / B08 / B03 后半。仅标记：BUGSCAN B02「E2E 垂直翻转」已实证为误报，建议关闭。
- **P1 建议（第 44 次提出）**：本自动化的巡查对象（C++ 重构）已消失 58 轮，建议改造为 Swift 主线巡查，或直接停用以免持续空转。
- 未修改本项目任何源文件。
- 18:20 C++ 巡查轮（停摆延续）：cpp/ 三源仍全缺，无新批次；未编译未冒烟未改源。git HEAD 260689b。
