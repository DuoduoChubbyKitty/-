# 2026-08-05 工作日志

## 项目整体盘点（用户要求"看一整个项目"）
- 定位：为游戏《异环》(NTE) 自动开车的 macOS 端到端 AI 辅助工具 AuroraDrive + 一套自动驾驶技术研究文库（约 100 篇 md，~100 万字）。
- 技术栈：纯 Swift 原生 macOS App（SwiftUI，macOS 14+），根目录 11 个 .swift 直接 swiftc 编译、无 Xcode 工程；CoreML 推理(m9_mono)；ScreenCaptureKit 截屏；CGEvent 注入 WASD。Python(src/, tools/) 仅离线训练层(DAgger 流程)。
- 核心管线 DriveState.tick() @30Hz：Capture→Inference(CoreML)→ConfidenceEst(启发式)→DegradeSTM(四态降级)→按态输出(E2E/Rule/YOLO/Escape)→Control(CGEvent)→游戏；RecordEngine 落盘、KeyboardMonitor 监听物理键。AuroraDriveApp.swift 把 11 个逻辑文件合并在一个 1100+ 行文件里。
- 模型：checkpoints/(696MB) 含 m9_mono/game_assist_control(含 _SAFE_best_epoch29_val0.4421.pt)/view_classifier 等；models/ 含 m9_mono 的 onnx/mlpackage/mlmodelc 三态；game_assist_yolo.onnx(489KB) 未接入。无 BEV/Transformer/UniAD 实装，仅研究文档。
- 研究文库：Apollo(30/30 完成)、OpenPilot(实际 30 篇已落盘，索引显示 5/30，滞后)、NVIDIA/ROS2/纯规则(各 10 章，待开始)、BEVFormer/UniAD(各 1 章，待开始)。索引文件 研究索引/00_index.md。
- 关键问题/技术债：
  1. YOLO 未接线（AuroraDriveApp.swift:331 `let detections: [Detection] = []`），`.rule`/`.yolo` 两态空转。
  2. 车速为伪造遥测（tick 第 3 步 `speed += (target-speed)*0.06 + random(-2.2...2.2)`），卡住检测与降级判据建立在假数据上。
  3. C++ Route B 已删除（cpp/frontend/src-tauri 整体移除，仅 backups/cpp_artifact_20260805/ 留 autodrive_core.cpython-311-darwin.so），大重构中途。
  4. configs/inference.yaml 指向不存在的 checkpoints/m9_deploy.pth，配置与当前单目路线脱节。
  5. 资源用 #filePath 硬编码，App 无法脱离源码目录分发。
  6. 研究索引状态已滞后于实际落盘文件。

## 全面代码审查（用户要求"全面审查代码"）
- 交付物：/Users/dupi/Desktop/自动驾驶系统/代码审查报告.md（464 行，P0×5 / P1×8 / P2×12）。
- 5 个 P0 阻断性问题：
  1. vehicle_state 训练/推理语义错位：训练 mono_dataset.py=[speed/120, curvature*5, sin/cos(heading), speed_limit/120, 0]，推理 InferenceEngine.buildVehicleState()=[speed, rpm(800-8000), gear(1-6), speed_norm, gear_norm, 0]，5/6 维错误，模型输入污染。
  2. 全部可运行源码未纳入 git 版本控制（git status：262 删除 / 32 未追踪 / 13 修改），重建/协作风险极高。
  3. 车速与 fps 为随机数伪造（AuroraDriveApp.swift tick 第 3 步），EscapeController 整文件为死代码。
  4. RecordEngine 录制格式与训练加载器不匹配（frames/00000.png 224×224 vs 加载器期望 frames/000001.jpg 180×320），录制数据全部被 _frame_path p.exists() 静默跳过。
  5. Python 导入断链：src/data_generator.py 引用已删除的 src/sensors.py / src/cpp_bridge.py（commit 260689b 删除），可从 git show 260689b^:src/sensors.py 恢复。
- 关键 P1：全局 CGEvent 注入无前台校验/无急停热键；主线程全分辨率重采样(averageBrightness O(W×H) 标注 O(1))；CaptureEngine 缩放未生效三方持有全分辨率位图；GameMapView 主线程同步加载 268MB PNG。
- 安全：未发现凭据泄露（值得肯定）；torch.load 建议 weights_only=True。
- 跨语言 I/O 契约无自动校验，是系统闭环断裂的根因，建议加 schema 自检 + 单测。

## 用户质疑后的深度复核（20:47-20:50）
用户质疑审查结论"全错"。当场翻代码逐条复核，结论修订：
- 属实（有代码+落盘数据铁证）：①车速/fps 随机数 AuroraDriveApp.swift:339-341（注释自认"遥测模拟"）；②detections=[] 未接线 :330；③录制格式不匹配——RecordEngine.swift:173 写 `%05d.png`，mono_dataset.py:135 期望 `%06d.jpg`，且 clip 目录名要求 `clip_*`（recordings/ 实际是 third_20260718_210236 等），扩展名/位数/分辨率/目录名四处全不对；④sensors.py/cpp_bridge.py 缺失属实（data_generator.py:38-40 引用）。
- 修正（原报告说过重）：①"训练管线断链"→ 实际只有旧 DAgger 脚本(train.py/dagger.py/trainer.py)断，当前 mono 训练 train_mono.py 不 import data_generator，不受影响；②vehicle_state"5/6维错误"→ 实为三处语义不一致（Swift[速度,rpm,gear,speed_norm,gear_norm,0] vs model.py:397 同 Swift vs mono_dataset.py:109-117 v2_new[速度/120,curvature,sin,cos,限速/120,0]），但 data/raw_clips 不在仓库，deployed m9_mono 实际训练分布无法证实，结论从"确定错误"降级为"高风险不一致"；③地图 268MB → 8192×8192 对（解码约268MB），但主图是 nte-game.jpg 3840×2160（1MB 磁盘），yihuan_map_z5.png(2.8MB) 是 fallback，表述不精确。
- 训练元数据：checkpoints/m9_mono/training_log.json 显示 image_size [180,320]（与推理一致）、仅 2 epoch、throttle/brake loss≈0（数据疑似退化，但数据不在仓库无法确证）。
- 沟通教训：用户反感"未运行App"措辞（被理解为"App不能用"）与 P0/P1 分级术语；用户要"简单易懂的表格+直接对质代码"。已承诺：有异议可随时现场翻代码核证。

## 地图底图修复任务（21:00-21:17）
目标：修地图底图、文字变清晰。关键事实（用户告知+已核实）：
- 地图数据时效：nteguide 系数据（stitched_map.png、nte_all_markers.json、FINAL_complete_map_database.json）是**约12年前的老地图**，现游戏地名/布局全变，不能用。
- 候选底图：①stitched_map.png(12年前,弃) ②game_map_manual.png(今天12:01-12:02由15张截图拼接,但道路没接好、整张乱掉,弃) ③**IMG_1366.jpeg(今天17:57,全国/整张世界地图截图,唯一可用)**。
- 标记点：JSON 里 5677 个来自12年前老数据 → 不能在当前底图上启用(filteredMarkers 保持 [] 是对的,只是原因写错了)。
- 超分可行性：①torch 在 py3.14 上 `pip install torch --dry-run` 直接 OOM(137)→ 真 ESRGAN 装不了；②opencv-python-headless 4.x/5.x 预编译包均已剔除 dnn_superres 子模块→ OpenCV 超分也不可用。本机唯一图像工具是生成式 ImageGen。
- 用 ImageGen 对 IMG_1366.jpeg 做 image-to-image(设 input_fidelity=high, prompt 强调不改字)→ 输出 models/Upscale_and_sharpen_this_game__2026-08-05T13-16-43.png，但**尺寸被压到 1024×1024(低于原图2159×2148)，且生成式有改字风险**→ 不能当底图。
- 重要限制：本会话模型不支持看图，无法替用户核验生成图文字。
- 待定：真正"字变清晰+不改字"需 Real-ESRGAN(torch)，本机装不了；可行路径=用户在有 torch 的机器跑 ESRGAN 后把图丢进来，或我先接受生成版让用户肉眼核验地名。
- **教训(已验证)**：用生成式 ImageGen 对 IMG_1366.jpeg 做 image-to-image，结果**整张图乱掉、文字被 AI 吃掉/乱编**——用户直接否决并要求回滚。结论：**绝不能用生成式图像工具处理地图/底图文字**（会毁字），只能用于纯创意出图。本机 faithful 超分(ESRGAN)装不了(torch OOM + OpenCV 剔除 dnn_superres)，故地图文字"变清晰"在本机唯一可行的是：①直接用原图当底图；②sips 双三次插值放大(忠实不改字但非去糊)；③用户在能跑 torch 的机器跑真 ESRGAN 后把图丢进来。
- **环境硬限制(已验证2次)**：本沙箱内存有上限，ML 运行时全装不上——`pip install torch --dry-run` OOM(137)、`pip install onnxruntime` 单独装也 OOM(137)。即**神经网络超分(torch+ESRGAN 与 onnxruntime+CoreML)在本机均不可行**。DLSS 也不行(Mac 无 NVIDIA)。故"字真正平滑锐利"只能由用户在内存足够的机器跑 ESRGAN 后把图丢回来。
- **venv 状态**：/tmp/srvenv 装了 numpy + opencv-python-headless 4.11(无 dnn_superres)。sips(系统内置)可做双三次放大，无需 python。
- **用户环境约束(用户口述)**：本机只有 3 核 / 3G 内存，跑不动 ML；用户另有一台**服务器**可用。我的 Bash 工具本身也在 ~3G 沙箱上限内，故任何需 pip 装 ML 运行时的方案在此都 OOM。结论：**地图超分应在用户服务器上跑**（小模型+切片），结果丢回本机接底图。
- **服务器方案(待用户给 OS/环境)**：在服务器 `pip install realesrgan` 后用 `python -m realesrgan.inference_realesrgan -n RealESRGAN_x4plus -i IMG_1366.jpeg -o enhanced.png --outscale 4 --tile 512`(--tile 512 即切片降内存)。用户把 enhanced.png 丢回，我接进 GameMapView。若服务器无 pip 可改用 docker run ESRGAN 镜像。
- **服务器也极其弱(用户口述 512MB 内存/42MB 显存, "42年前")**：连小模型都跑不动。结论：**用户全部自有硬件(本机3G沙箱+512MB服务器)都无法跑任何神经网络超分**。真·去糊只剩两条路：①借/租有≥4G内存的机器跑 ESRGAN；②本机用 sips 双三次放大(忠实不改字、非去糊,唯一能跑的)。
